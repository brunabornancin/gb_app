extends ../layout

block content

	#top.main-pages
		.bg-pages#events
		.image-overlay.header-text
			.caption
				h2 News & Events

	.container
		.row
			.col-md-12
				.section-heading
					h2 Latest News

	.container
		.row
			each article in articles
				.col-md-4.mt-4
					.card-mt-4
						.card-body.news-thumb
							.card-title=article.title
							.card-subtitle.text-muted.mb-2=article.createdAt.toLocaleDateString('en-CA')
							.card-text.mb-2=article.description
							a.btn.btn-primary.mt-4(href="articles/article.slug") Read More